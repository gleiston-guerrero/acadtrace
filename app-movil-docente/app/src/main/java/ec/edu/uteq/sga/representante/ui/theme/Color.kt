package ec.edu.uteq.sga.representante.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta oficial del Microservicio Docente y SGA Principal
val PrimaryNavy = Color(0xFF243A76)       // Color institucional #243A76
val PrimaryNavyLight = Color(0xFF2D4A96)  // Header chips / botones secundarios
val AccentGreen = Color(0xFF16A34A)       // Aprobado / Presente / Éxito
val DangerRed = Color(0xFFDC2626)         // Alerta / Reprobado / Ausente
val WarningAmber = Color(0xFFD97706)      // En riesgo / Justificado / Pendiente

val BackgroundSlate = Color(0xFFF8FAFC)   // bg-slate-50
val CardSurface = Color(0xFFFFFFFF)       // Tarjetas blancas limpias
val SlateBorder = Color(0xFFE2E8F0)       // border-slate-200
val TextPrimary = Color(0xFF1E293B)       // text-slate-800
val TextSecondary = Color(0xFF64748B)     // text-slate-500
val TextMuted = Color(0xFF94A3B8)         // text-slate-400

// Theme colors
val SurfaceLight = Color(0xFFFFFFFF)
val BackgroundLight = Color(0xFFF8FAFC)
val TextPrimaryLight = Color(0xFF1E293B)

val SurfaceDark = Color(0xFF1E293B)
val BackgroundDark = Color(0xFF0F172A)
val TextPrimaryDark = Color(0xFFF8FAFC)

// Colores de módulos (idénticos a microservicio-docente/src/config/modulos.jsx)
val ModuloAulaVirtualBg = Color(0xFFEEF2FF)
val ModuloAulaVirtualText = Color(0xFF4F46E5)

val ModuloActividadesBg = Color(0xFFEFF6FF)
val ModuloActividadesText = Color(0xFF2563EB)

val ModuloAsistenciaBg = Color(0xFFF0FDF4)
val ModuloAsistenciaText = Color(0xFF16A34A)

val ModuloCalificacionesBg = Color(0xFFFAF5FF)
val ModuloCalificacionesText = Color(0xFF9333EA)

val ModuloSeguimientoBg = Color(0xFFF0FDFA)
val ModuloSeguimientoText = Color(0xFF0D9488)

val ModuloReportesBg = Color(0xFFFFF1F2)
val ModuloReportesText = Color(0xFFE11D48)

val ModuloAnunciosBg = Color(0xFFFFFBEB)
val ModuloAnunciosText = Color(0xFFD97706)

val ModuloMaterialBg = Color(0xFFECFEFF)
val ModuloMaterialText = Color(0xFF0891B2)

val ModuloHorarioBg = Color(0xFFF0F9FF)
val ModuloHorarioText = Color(0xFF0284C7)

// Asistencias chips
val ChipPresente = Color(0xFF16A34A)
val ChipAusente = Color(0xFFDC2626)
val ChipJustificado = Color(0xFFD97706)
val ChipAtraso = Color(0xFFEA580C)

// Backward compatibility references
val PrimaryBlue = PrimaryNavy
val SecondarySky = PrimaryNavyLight
val DarkNavy = PrimaryNavy
val LightGray = BackgroundSlate
val CardBackground = CardSurface
