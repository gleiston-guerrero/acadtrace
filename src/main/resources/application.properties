spring.application.name=sga-soporte

# =============================================
#  SGA - Microservicio de Soporte T?cnico
# =============================================
server.port=8081

# Base de datos Supabase
spring.datasource.url=jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres
spring.datasource.username=postgres.bxqixhhgkcyojulbmhpp
spring.datasource.password=402/42745aA
spring.datasource.driver-class-name=org.postgresql.Driver

# Pool de conexiones limitado (Supabase free tier)
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.default_schema=sga_principal
spring.jpa.properties.hibernate.type.preferred_uuid_jdbc_type=CHAR

# JWT ? debe ser EXACTAMENTE igual al de sga-principal
# sga-principal:
jwt.secret=E46_REMOVED_CREDENTIAL
# sga-soporte (este archivo):
jwt.expiration=86400000

spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration

# Exponer /actuator/health para que el dashboard de soporte pueda monitorear este servicio
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=never